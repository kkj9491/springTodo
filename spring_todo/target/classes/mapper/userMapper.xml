<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="org.test.UserMapper">

	<insert id="create" >
		insert into todo_user (id, password, name, email) values
		(#{id}, #{password}, #{name}, #{email})
	</insert>
	
	<select id="read" resultType="org.test.model.UserDAO">
		select * from todo_user where id=#{id}
	</select>
	
	<update id="update">
		update todo_user set title=#{title}, content=#{content}
		where bno=#{bno}
	</update>
	
	<delete id="delete">
		delete from todo_user where bno=#{bno}
	</delete>
	
	<select id="listAll" resultType="org.study.model.UserVO">
	<![CDATA[
		select * from todo_user
		where bno > 0
		order by bno desc, regdate desc
	]]>	
	</select>	

</mapper>